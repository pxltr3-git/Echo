---
import * as icons from '../data/svgs.js';

export interface Props {
    icon: keyof typeof icons;
    class?: string;
    [key: string]: any;
}

const { icon, ...rest } = Astro.props;
const SvgComponent = icons[icon];
---

<SvgComponent {...rest} />